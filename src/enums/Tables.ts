enum Tables {
  TEMPORARY_USERS = "temporary_users",
  USERS = "users",
  QUESTIONS = "questions",
  ANSWERS = "answers", 

  SMS_SEND_LOGS = "sms_send_logs",
  CRON_RUNS = "cron_runs",


  WALLETS = "wallets",
  
}

export default Tables;
